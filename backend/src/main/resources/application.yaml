spring:
  application:
    name: bda

  config:
    import: optional:file:.env[.properties],optional:file:../.env[.properties]

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bda}
    username: ${DB_USER:masterpiece}
    password: ${DB_PASS:comebaaaaaaaaaaaaaaack}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    sql-migration-prefix: v
    baseline-on-migrate: true
    baseline-version: 0

server:
  port: 8080
  servlet:
    context-path: /api

version:
  path: v1

jwt:
  # Environment value is preferred; fallback is dev-only.
  secret: ${JWT_SECRET:dev-only-change-me-dev-only-change-me-dev-only-change-me-32-bytes-min}
  expiration-ms: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs.yaml
  api-docs:
    path: /v3/api-docs
  packages-to-scan: com.bda.bda.controller.v1
  paths-to-match: /v1/**
